#redirectors used to control the input and output of commands. it allows to redirect standard input and standard output and error messgae

#redirect the output , overwrite if file exists
#ls > output.txt

#redirect the output , append if file exists
ls >> output.txt